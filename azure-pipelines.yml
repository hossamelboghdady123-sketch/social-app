trigger: none

pool:
  name: Default

steps:
- bash: |
    echo "HELLO HOSSAM"
    echo "PIPELINE VERSION 2"
    echo "Agent: $AGENT_NAME"
    hostname
    whoami
    pwd
    python3 --version
    docker --version
  displayName: 'TEST PIPELINE'
