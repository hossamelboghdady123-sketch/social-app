trigger: none

pool:
  name: Default

steps:
- bash: |
    echo "=========================="
    echo "HELLO FROM LOCAL AGENT"
    echo "=========================="
    hostname
    whoami
    pwd
    python3 --version
    docker --version
  displayName: Test Local Agent